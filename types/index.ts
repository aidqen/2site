export interface Category {
  id: string;
  title: string;
  imgUrl: string;
  sectionId: string;
  description: string;
}

export interface Section {
  id: string;
  title: string;
  description: string;
}
